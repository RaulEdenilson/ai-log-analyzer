# Umbral global para marcar anomalías (0.0 – 1.0)
ANOMALY_THRESHOLD=0.7

# Latencia (ms) a partir de la cual suma peso al score
LATENCY_MS_THRESHOLD=800

# Ruta SQLite (apunta a /app/data que mapeamos con volumen)
SQLITE_URL=sqlite:///./data/data.db
